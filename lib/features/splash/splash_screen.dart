import 'package:flutter/material.dart';
import 'package:rentmate/theme/colorschemes.dart';

class SplashScreen extends StatelessWidget {
  const SplashScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: ,
    )
  }
}
